using UnityEngine;
using System.Collections;

public class LoadNextLevel : MonoBehaviour {

	// Use this for initialization
	void LoadLevel() {
		//if scene is then next scene.
		//down't show if last level
	}
}
